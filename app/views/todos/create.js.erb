$("<%=escape_javascript(render @todo) %>").appendTo("#todos-table");
$('.todo-form').html('<%= link_to new_todo_path, remote: true, class: "btn btn-success" do %> <i class="glyphicon glyphicon-plus"></i> New Todo <% end %>');
